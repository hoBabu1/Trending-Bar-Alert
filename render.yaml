services:
  - type: worker
    name: candle-alert-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python candle_alert_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
